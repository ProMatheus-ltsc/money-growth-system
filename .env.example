# ============ fam-asset-tracker 环境变量示例 ============
# 本项目按 04 §7.1 定论【无静态密钥】：
#  - 无 JWT 签名密钥（会话为 D1 数据库令牌模式）
#  - 无第三方 API Key（外部 AI 为手工复制粘贴，零直连）
#  - 管理员/只读账号不落代码与配置（红线 R2），
#    首次部署后经 POST /api/auth/init 由用户设置
#
# 以下仅为部署辅助变量（非密钥），本地开发无需填写：

# Cloudflare 账号 ID（S10 部署时由 `wrangler whoami` 获取后填写；仅 CI/部署机需要）
CLOUDFLARE_ACCOUNT_ID=

# Cloudflare API Token（仅部署机使用；建议用 wrangler login 代替，不落盘）
# CLOUDFLARE_API_TOKEN=

# 登录前置门控邀请码（CR-004 修复：不入仓库）：
#  - 本地开发：复制为 .dev.vars 写入 INVITE_CODE=你的邀请码
#  - 线上：`npx wrangler secret put INVITE_CODE`
#  - 本文件不留默认值（红线 R2）
# INVITE_CODE=

# 注意：D1 database_id 由 `wrangler d1 create fam-asset-db` 生成后回填到
# wrangler.toml 的 [[d1_databases]]，不放环境变量。
